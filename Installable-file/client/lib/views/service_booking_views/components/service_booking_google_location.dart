import 'package:flutter/material.dart';

class ServiceBookingGoogleLocation extends StatelessWidget {
  const ServiceBookingGoogleLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
